# 게임 요청 히스토리

## 원래 요청 의도

웹캠으로 손가락을 인식해서 플레이하는 웹 게임을 만들고자 했다.
화면 위쪽에서 블록이 떨어지고, 화면 아래쪽에서도 블록이 올라옴, 카메라로 인식한 검지 손가락을 움직여 블록을 맞히면 점수가 오르는 방식이다.
블록 모양을 귀여운 동물 발로 만들어줘
## 게임 콘셉트

- 게임 이름: Finger Beat Blocks
- 장르: 손 인식 리듬/반응형 블록 게임
- 실행 방식: 브라우저에서 `index.html`로 실행
- 핵심 입력: 웹캠으로 인식한 검지 손가락 위치
- 보조 입력: 카메라 없이 테스트할 수 있는 마우스 연습 모드

## 주요 규칙

- 블록은 화면 위에서 아래, 아래에서 위로 떨어진다.
- 플레이어는 검지 손가락을 움직여 떨어지는 블록을 맞힌다.
- 블록을 맞히면 점수가 올라간다.
- 연속으로 맞히면 콤보가 증가한다.
- 블록을 놓치면 '아이쿠' 소리를 낸다.
- 미스 시 점수가 일부 감소한다.

## 화면 구성

- 어두운 네온 스타일 배경
- 블록이 떨어지는 여러 개의 세로 레인
- 점수, 콤보, 미스 표시
- 시작 화면 오버레이
- 카메라 시작 버튼
- 마우스 연습 버튼
- 안내 토스트 메시지

## 조작 방식

- 카메라 모드: MediaPipe Hands로 검지 끝 위치를 추적해서 포인터로 사용
- 연습 모드: 마우스 위치를 포인터로 사용
- 스페이스바: 게임 일시정지 또는 재개

## 사용 기술

- HTML
- CSS
- JavaScript
- Canvas 2D
- MediaPipe Hands
- MediaPipe Camera Utils

## 현재 상태 메모

현재 `index.html`에는 게임 코드가 들어가 있지만, 일부 한글 텍스트가 인코딩 문제로 깨져 있다.
또한 `startButton.textContent`가 들어간 줄에 문자열 따옴표가 닫히지 않아 JavaScript 문법 오류가 발생할 수 있다.

수정이 필요한 대표 예시는 다음과 같다.

```js
startButton.textContent = "카메라 준비 중...";
```

